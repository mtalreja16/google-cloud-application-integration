{
    "name": "${pubsubconnector}",
    "connectorDetails": {
        "name": "pubsub",
        "version": 1 
    },
    "configVariables": [
        {
            "key": "project_id",
            "stringValue": "${project}"
        },
        {
            "key": "topic_id",
            "stringValue": "${pubsubconnector}"
        }
    ],
    "serviceAccount":"${service_account_name}@${project}.iam.gserviceaccount.com"
}